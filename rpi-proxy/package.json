{
  "name": "rpi-simple-proxy",
  "version": "4.0.0",
  "description": "Residential IP proxy — TypeScript rewrite with modular architecture",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "start": "tsc && node dist/index.js",
    "start:bun": "bun run src/index.ts",
    "start:legacy": "node server.js",
    "dev": "bun run --watch src/index.ts"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  }
}
